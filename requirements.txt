# --- Core Backend Framework ---
fastapi==0.110.0
uvicorn[standard]==0.29.0

# --- PDF Processing ---
PyPDF2==3.0.1
pdfminer.six==20221105

# --- Machine Learning / Embeddings ---
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
sentence-transformers==2.7.0       # uses torch under the hood; CPU-only via index above
torch==2.3.1                       # CPU-only wheel from PyTorch CPU index

# --- Vector Database (Qdrant) ---
qdrant-client==1.7.3

# --- Database ORM (for metadata & query history) ---
SQLAlchemy==2.0.29
psycopg2-binary==2.9.9

# --- HTTP client ---
requests==2.31.0

# --- File Upload Support ---
python-multipart>=0.0.6